# Manoel Island Sandbox

Local admin password for development (override in `.env`):

```
VITE_ADMIN_PASSWORD=manoel-admin
```

Admin panel shortcut: `P`

Default password: `z1z1z1z1` (override with `VITE_ADMIN_PASSWORD` in `.env`)
